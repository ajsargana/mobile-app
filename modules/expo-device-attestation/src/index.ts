// Re-exports for autolinking discovery. The real module is loaded by the app
// via `requireNativeModule('ExpoDeviceAttestation')` from src/lib/attestation.
export { default } from './ExpoDeviceAttestationModule';
